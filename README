DOMINION INDEX APP

SETUP:

   install with:
   git clone https://github.com/notevenodd/dominion-index.git

   call the ./install script, this will:
   1) clone the dominion randomizer (KingdomCreator on GitHub) from github
   2) set up the 'app' directory

TO ACCESS THE APP:

   1) use the 'server' script (check its usage)
   2) use the browser (make sure you point to the 'dominion.html' page)
