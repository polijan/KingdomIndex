DOMINION INDEX APP

SETUP:
   call the ./install script, this will:
   1) clone the dominion randomizer (KingdomCreator on GitHub) from github
   2) set up the 'app' directory
   3) set up ~/.dominion-index personal directroy where boards are stored

TO GENERATE A BOARD:
   use mkboard script (pass the cards as parameters)

TO ACCESS THE APP:
   serve the 'app' on a http server
   point the browser at 'dominion.html' on the served URL.
